# 一体式筛选底栏检查

- 设计来源：`/Users/zgx/.codex/generated_images/01a09fd8-3132-7931-b2c5-ca1a0a801032/exec-3732edb2-9817-4058-a47f-405ca89e79b1.png`（1774 × 887 概念画布）。
- 实现：`http://localhost:5173/`，桌面视口 1440 × 1000、窄屏视口 800 × 1000，截图密度均为 1。
- 桌面截图：`/tmp/search-footer-desktop-final.png`。
- 窄屏截图：`/tmp/search-footer-narrow.png`。
- 下拉修复截图：`/tmp/search-footer-menu-fixed.png`。
- 状态：筛选区展开，地区已选北京；另检查学历多选、单位性质多选、移除条件、清空、Escape 关闭。

## 对比与结果

概念图和实现截图在同一轮图像输入中检查，按搜索卡片区域比较，不把概念画布的标题、外部留白作为页面要求。源图卡片宽约 1445 像素，实现约 1032 CSS 像素，按相对比例评估布局，无逐像素一致性声明。底栏文字与分隔线在全尺寸截图中可读，无需额外局部放大。

- 字体：沿用项目中文无衬线字体，标签 12px、当前值 16px，长值截断并保留完整可访问名称和 title。
- 布局：底栏贴齐卡片左右与底部，五列共享浅色背景和顶部边界，短竖线分隔；800px 时三列换行，无新增横向溢出。
- 颜色：使用现有 secondary、border、interaction 令牌；选中值蓝色，默认值深色，保留明确的选中状态区别。
- 图像：该变更无新增位图需求，箭头沿用 Lucide。
- 内容：保留全部原有选项、热门城市与专业、招聘方向、已选条件功能；概念图省略的原有功能继续保留，属于本次组件改造的范围约束。

## 迭代记录

1. 发现 P2：城市下拉列表过高，向上弹出时首项被站点固定导航遮挡，无法点击北京。证据：`/tmp/search-menu.png`。
2. 修复：菜单最大高度设为 320px，并保留 16px 视口避让；列表可滚动。
3. 复查：北京可点击并回显；修复截图中菜单处于 y=299–619，未被导航遮挡。没有剩余可操作的 P0/P1/P2 问题。
4. 根据反馈收紧底栏上下留白和标签间距，浏览器测量桌面底栏高 77px、按钮高 60px。以上截图对应收紧前版本；此轮通过 DOM 尺寸和格式检查验证。

构建、组件 ESLint、Prettier、git diff 空白检查通过。未新增测试用例。未单独采集浏览器控制台；未改动全站既有的 680px 最小宽度规则。

final result: passed
